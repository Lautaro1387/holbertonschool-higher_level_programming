#!/usr/bin/python3
def new_in_list(my_list, idx, element):
    my_list.copy()
    for idx in range(len(my_list)):
        if idx < 0:
            return my_list[idx]
        if idx > (len(my_list)):
            return my_list[idx]
        return my_list.copy()
        
