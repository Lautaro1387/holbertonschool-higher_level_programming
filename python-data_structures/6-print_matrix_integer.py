#!/usr/bin/python3
def print_matrix_integer(matrix=[[]]):
    for number in range(len(matrix)):
        for number2 in range(len(matrix))
            print("{:d}".format(matrix[number][number2]), end=" ")
